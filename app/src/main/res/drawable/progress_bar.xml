<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 这个是背景颜色 -->
    <item android:id="@android:id/background">
        <shape>

            <!-- 这个是设置圆角还会直角，如果设置成0dip就是直角 -->
            <corners android:radius="5dp" />
            <!-- 设置背景的颜色，将startColor、centerColor、endColor都设置成同一个颜色值 -->
            <gradient
                android:angle="270"
                android:centerColor="#E3E3E3"
                android:centerY="0.75"
                android:endColor="#E3E3E3"
                android:startColor="#E3E3E3" />
        </shape>
    </item>
    <!-- 这个是设置你进度的颜色 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>

                <!-- 这个也是设置时圆角还是直角，左边的 -->
                <corners android:radius="5dp" />
                <!-- 设置进度的颜色，将startColor、centerColor、endColor都设置成同一个颜色值 -->
                <gradient
                    android:angle="270"
                    android:centerColor="#E43E0A"
                    android:endColor="#E43E0A"
                    android:startColor="#E43E0A" />
            </shape>
        </clip>
    </item>

</layer-list>